---
scope: YouTube Organizer — whole product; Phase 1 build substrate with Phase 2 headroom. Governs backend (Django/DRF) + frontend (Next.js) + the YouTube sync engine.
purpose: build-substrate
altitude: feature (keeps epics coherent)
updated: 2026-07-24T12:35
---

- (event) Run start. Inputs: PRD (prd-youtube-organizer-2026-07-20), UX spines DESIGN.md + EXPERIENCE.md (2026-07-23), project-context.md, Docs/FRONTEND-STACK.md, Docs/CI-AND-GITHUB-GATES.md, plus a code sweep of backend/ and frontend/. Mode: Coaching path. Deliverables: spine + epic/work-split view.
- (constraint) Code reality (swept, not assumed): backend/organizer is an auth stub only — models.py holds just UserSocialToken (OneToOne User, tokens+scope); views.py holds only MeView; urls.py has 4 routes; google_auth_views.py SCOPES is youtube.readonly + openid + email + profile (NO write scope yet, FR-1 needs it). organizer/tests.py is empty. One migration (0001_initial). No Video/Tag/Playlist/Library model exists.
- (constraint) Code reality: frontend/ is a legacy prototype — Next 15.4.6, React 19.1.0, Tailwind v4 (@tailwindcss/postcss), FontAwesome icons, CSS Modules + plain CSS, raw fetch via app/hooks/usePlaylists.ts, npm + package-lock.json, PascalCase filenames. Docs/FRONTEND-STACK.md disowns ALL of it. Effectively greenfield frontend.
- (constraint) Authoritative and binding (CLAUDE.md + project-context.md): Docs/FRONTEND-STACK.md and Docs/CI-AND-GITHUB-GATES.md win over existing code. Never add AI/bot attribution to any artifact. Git flow: feat/story-* -> PR -> develop (squash) -> main (merge + vX.Y.Z tag); never commit direct to main/develop.
- (constraint) PRD binding: single-user (Alexis only, no multi-user/sharing/public surface in any phase). Phase 1 = FR-1,2,5,6,7,9,11,12,13(non-date),14,15,16 + responsive desktop+mobile. Phase 2 = FR-3,4,8,10,17,18,19 + date-range filters + tag-merge.
- (constraint) PRD NFR (load-bearing, SM-C2): triage throughput bound — filing one video <=2 interactions; applying a tag/attribute to a multi-selection is ONE action. Incremental by design — Uncategorized never needs emptying; partially-triaged is first-class and never blocks browse/search/playback.
- (constraint) PRD FR-6 ordering invariant (crash-safety): import -> persist+verify locally -> only THEN remove from _Inbox on YouTube. A failed import is never removed. An imported-but-uncleared video is a tracked pending-clear ORPHAN retried on next sync, never skipped as a duplicate. Removal failures surfaced, never silent.
- (constraint) PRD FR-5/FR-11: dedupe by YouTube video ID; a permanently-purged video leaves a lightweight TOMBSTONE so it is not resurrected by a later import while still sitting in _Inbox. Delete goes to a recoverable Trash with a retention window before permanent purge. Deletion is app-side only, never touches YouTube.
- (constraint) PRD glossary: Library is the canonical set of ALL captured Videos; Tags and Playlists are optional overlays that never determine existence. Uncategorized is a DERIVED view (no tags AND no playlist membership), self-clearing, a state not a container.
- (constraint) UX EXPERIENCE.md binding: sync runs UNATTENDED in the background (UJ-2: background sync has already pulled the week's inbox without him asking); Sync status is a read-only surface over a background process reporting last successful sync, next scheduled run, and pending-clear orphans. Quota-exhausted banner must persist until acknowledged because sync is unattended.
- (constraint) UX EXPERIENCE.md binding: infinite scroll is BANNED everywhere (pagination or explicit load-more only) — it is the doomscroll delivery mechanism. Autoplay-next banned. Filter state lives in the URL. Tag application is OPTIMISTIC, reconciling in background, reverting only affected rows on failure.
- (constraint) UX DESIGN.md binding: shadcn/ui neutral on Tailwind v3; no brand/accent hue at all; destructive red reserved for destruction only; Geist Sans via next/font. This spine does not re-decide visual tokens — DESIGN.md owns them.
- (constraint) Security debt flagged by project-context.md, unresolved: settings.py has DEBUG=True, hardcoded SECRET_KEY, empty ALLOWED_HOSTS. CI files (.github/workflows/ci.yml, deploy.yml, pull_request_template.md) are unadapted Accountr placeholders assuming Nx+pnpm+Prisma and apps/**/libs/** paths — they do not fit this repo. No docker-compose.prod.yml exists.
- (constraint) Auth invariant (do not break): JWT in HttpOnly access_token cookie + JWTAuthCookieMiddleware injecting Authorization: Bearer before DRF. NOT header-based from the client. CORS_ALLOW_CREDENTIALS=True, origin https://localhost:3000 only; session/CSRF cookies SameSite=None; Secure; HTTPS on localhost everywhere.
- (version) Web-verified 2026-07-24: Django 6.0 released 2025-12-03; 6.0.7 current stable (2026-07-07); supports Python 3.12-3.14. Repo currently runs Django 5.2 with requirements.txt pinning only 'Django>=4.0'.
- (version) Web-verified 2026-07-24 (docs.djangoproject.com/en/6.0/topics/tasks/): Django 6.0's built-in Tasks framework ships ONLY ImmediateBackend + DummyBackend, both dev/test only. Docs state 'It does not provide a worker mechanism to run Tasks. The actual execution must be handled by infrastructure outside Django.' No scheduler, no durable DB-backed queue, no retry in core. Therefore django.tasks alone CANNOT satisfy the unattended background sync — it is an API contract, not a runtime.
- (version) Web-verified 2026-07-24: Next.js 16.2.11 is the current LTS line and a Next.js security release shipped July 2026. Docs/FRONTEND-STACK.md pins '~16.1.6' (16.1.x only), which is behind the LTS line and may miss the July 2026 security fix. Nx 22.7.5 is current and supports Next 16.
- (direction) Key architectural observation driving the sync-runtime choice: FR-6 pending-clear orphans are a DOMAIN entity, not a broker implementation detail — EXPERIENCE.md requires a user-visible banner ('3 videos imported but not removed from _Inbox. Retrying next sync.') and a Settings orphan list. Retry state must live in app tables regardless of runner. This shrinks the job-runner requirement to 'run periodically + allow manual trigger'.
- (decision) REPO SHAPE (user chose): keep the two-tree repo — frontend/ + backend/ as-is. No Nx, no pnpm workspaces, no apps/**/libs/** restructure. Binds: all frontend and CI work. Prevents: a story inventing a monorepo layout mid-stream. Alternatives weighed: (a) full Nx 22.7.5 + pnpm workspaces per FRONTEND-STACK.md, (b) pnpm workspaces without Nx. Rationale for rejecting: Nx's affected-graph and caching earn nothing on a single Next app with a Python backend Nx cannot manage; the ceremony is unpaid-for on a solo personal tool.
- (constraint) CONSEQUENCE of the repo-shape decision — three upstream docs now diverge from reality and must be reconciled, not silently ignored: (1) Docs/FRONTEND-STACK.md §1 mandates Nx 22.7.5 + pnpm workspaces; (2) Docs/CI-AND-GITHUB-GATES.md pre-push hook runs 'nx run-many -t test/lint/build' which cannot work here, and CI paths target apps/**/libs/**/nx.json/pnpm-lock.yaml which do not exist; (3) FRONTEND-STACK.md §2 designates 'a shared TypeScript types package' as the single source of truth for API shapes — a mechanism the two-tree repo removes, leaving the API type contract UNDECIDED and requiring its own AD.
- (decision) SYNC RUNTIME (user chose): a plain idempotent Django management command (manage.py sync_inbox) fired weekly by a scheduler, with the same command reachable from a manual trigger in Settings. No broker, no worker daemon. All batch, orphan, and quota state lives in first-class Postgres rows the UI reads directly. Binds: FR-5, FR-6, FR-19, and Phase-2 FR-3/FR-4. Prevents: crash-safety being delegated to a queue's internal retry semantics that the UI cannot inspect. Alternatives weighed: django-q2 (ORM broker + scheduler), Celery+Redis+beat, Django 6.0 django.tasks + third-party worker backend.
- (decision) BACKEND LAYERING (user chose): layered with an explicit YouTube port. organizer/api (DRF views + serializers, HTTP concerns only) -> organizer/services (domain operations, the ONLY layer that mutates state) -> organizer/models. Every YouTube Data API call goes through a single organizer/youtube/ gateway with a fake implementation used by tests. Views never call Google; services never handle HTTP. Binds: all backend work. Prevents: quota handling, retry, and crash-safe ordering scattering across views, and a backend that cannot be tested without the network. Alternatives weighed: full hexagonal with ORM repositories (rejected — fights Django's grain for a single-user tool), fat views (rejected — status quo has zero tests and nowhere for the sync engine to live).
- (decision) API TYPE CONTRACT (user chose): DRF serializers are the single source of truth; an OpenAPI schema is generated from them and TypeScript types are generated from the schema into the frontend. A CI step regenerates and fails the build on drift. Binds: every endpoint and every frontend data hook. Prevents: the triage epic and the browse epic each hand-writing a Video type and drifting on the first nullable field — the divergence the now-absent shared-types package used to prevent.
- (decision) SYNC LIFECYCLE MODEL (user chose): TRANSACTIONAL OUTBOX + SyncRun. A PendingYouTubeOp row (op type, target ids, attempts, last_error, state) is written in the SAME database transaction that commits the Video; a drain step is the only code path that issues YouTube WRITE calls. Pending-clear orphans (FR-6) are simply undrained rows. A SyncRun row per run (started, finished, counts, quota_exhausted_at, acknowledged_at) backs the sync-status surface, the partial-failure banner, and the quota banner that must persist until acknowledged. Phase-2 FR-4 playlist deletion reuses the same outbox.
- (decision) BROWSE/QUERY CONTRACT (user chose): ONE list endpoint (GET /api/videos/) with a single fixed, documented query-param grammar owns every browse surface — Uncategorized, Tag detail, Search, Filters panel, Playlist detail, Needs review. Uncategorized is derived server-side from params, NEVER a stored flag (PRD glossary: it is a state, not a container). Page-number pagination carrying a total count (shelf rows need 'guitar · 24'); explicit load-more; infinite scroll banned. Frontend URL params map 1:1 onto API params, satisfying EXPERIENCE.md 'filter state lives in the URL'.
- (decision) SEARCH (user chose): PostgreSQL full-text search — a maintained tsvector column spanning title + description + channel name + tags, GIN-indexed, queried through Django's SearchVector/SearchQuery. Stays inside Postgres, no new service. Rejected: plain ILIKE (no ranking, sequential scan over long descriptions), pg_trgm (weaker on description text).
- (version) Web-verified 2026-07-24: Django REST Framework supports Django 4.2, 5.0, 5.1, 5.2 AND 6.0; DRF 3.16.1 is confirmed Django 6.0-compatible. A Django 5.2 -> 6.0 bump is therefore not blocked by DRF.
- (version) Web-verified 2026-07-24: drf-spectacular 0.30.0 released 2026-07-06 — current and actively maintained.
- (version) Web-verified 2026-07-24 — RISK: openapi-typescript is at 7.13.0 with no release since February 2026; community discussions report unreviewed PRs and TypeScript 6 support blocked, raising maintenance-stall concerns. Flagged to the user rather than bound silently; hey-api/openapi-ts surfaced as the actively-released alternative.
- (decision) CODEGEN TOOL (user chose): @hey-api/openapi-ts generates the TypeScript types from the drf-spectacular OpenAPI schema. Chosen over openapi-typescript 7.13.0 specifically because of the verified maintenance-stall signals (no release since Feb 2026, TS 6 support blocked) — the generator sits inside the CI drift gate and must not be a stalling dependency.
- (decision) DEPLOYMENT ENVELOPE (user chose): LOCALHOST ONLY for Phase 1. Dev docker-compose is the only environment. No docker-compose.prod.yml, no VPS, no PaaS in Phase 1; the whole production envelope (settings split, env SECRET_KEY, DEBUG off, ALLOWED_HOSTS, real TLS hostname, retargeted deploy.yml) is deferred to Phase 2. Alternatives weighed: always-on VPS/home server, managed PaaS (Fly/Railway/Render) with a native cron primitive.
- (constraint) ACCEPTED PHASE-1 LIMITATION arising from localhost-only: unattended background sync is NOT truly unattended — nothing runs when the stack is down. EXPERIENCE.md UJ-2 ('background sync has already pulled the week's inbox without him asking') is therefore only conditionally honored in Phase 1. This must be stated in the spine as an accepted limitation, not discovered during the sync epic. It joins the two already-accepted platform limitations (PRD R2 background playback, and the embed end-screen recommendation leak).
- (decision) DJANGO VERSION (user chose): bump Django 5.2 -> 6.0.x (6.0.7 current) NOW, while the backend is still an auth stub and there is no Video/Tag/sync code to migrate — the cheapest the upgrade will ever be. DRF 3.16.1 verified Django 6.0-compatible. Every requirement gets a bounded pin, replacing the unbounded 'Django>=4.0' footgun in requirements.txt.
- (decision) FRONTEND DATA LAYER (user chose): a single query-key factory owns the whole React Query key hierarchy (['videos','list',params] / ['videos','detail',id] / ['tags','list'] etc.) — no hook invents its own key. Every mutation colocates its onMutate snapshot, optimistic patch, PER-ITEM onError rollback, and settled invalidation with the hook. The governing case: bulk tag in Uncategorized optimistically REMOVES rows from the uncategorized list cache (self-clearing derived view) and on partial failure restores only the failed ids, never the whole sweep. Rejected: ad-hoc keys with broad prefix invalidation (refetch re-orders a triage list under the cursor mid-sweep), and a hand-built normalized entity cache (too much machinery for a single-user tool).
- (decision) SYNC TRIGGER (user chose, after the unattended-premise trade-off was put in front of them): MANUAL TRIGGER ONLY in Phase 1. sync_inbox runs when the user presses 'Sync now' in Settings. No scheduler service, no host cron, no interval loop. Alternatives weighed and declined: catch-up-on-stack-start + interval while up + manual; host-level cron/launchd.
- (constraint) CONFLICT WITH SOURCE INPUT to surface, not absorb: manual-trigger-only contradicts EXPERIENCE.md UJ-2 step 1 ('Background sync has already pulled the week's inbox - 94 videos - without him asking') and the Sync status surface which promises 'next scheduled run'. Offer to amend EXPERIENCE.md at Finalize so the UX spine and the architecture spine do not silently diverge. The state-pattern rows for sync (running/complete/partial-failure/quota/failed) all remain valid — only the TRIGGER changes from scheduled to user-initiated.
- (decision) WATCHED TRACKING (user chose): threshold + CONTIGUOUS-WATCH CHECK. The client tracks actually-covered playback segments via the YouTube IFrame Player API and the threshold is evaluated against covered duration, not maximum position reached, so seeking to the end does not mark a video watched. One PATCH when the threshold is genuinely crossed; no playback position is persisted (FR-16 asks for a badge and a manual toggle, never a resume position). This RESOLVES PRD Open Question 7 in favour of accuracy over simplicity. Rejected: naive max-position threshold, continuous position persistence.
- (decision) TRASH / TOMBSTONE (user chose): soft-delete via Video.deleted_at drives Trash and is excluded from every default queryset; permanent purge hard-deletes the row and writes VideoTombstone(youtube_id, purged_at). THE INVARIANT IS THE DEDUPE CHECK, not the tables: import dedupe must test active videos AND soft-deleted videos AND tombstones, or a video sitting in Trash is re-imported into Uncategorized on the next sync — precisely the resurrection FR-5 forbids.
- (decision) QUOTA (user chose): reactive detection plus a per-run item cap. Detect YouTube's 403 quotaExceeded on the failing call, stamp quota_exhausted_at on the SyncRun, commit everything already done, resume from the same point on the next run. A configurable max-items-per-run prevents a large first import burning the daily budget in one pass. Answers PRD Open Question 6 (quota exhaustion mid-triage). Rejected: a predictive unit ledger (estimates drift from Google's real accounting), reactive-only with no cap.
- (event) Coaching complete — 13 decisions captured across 4 rounds. Proceeding to Finalize step 1 (distill the spine from the memlog).
- (event) Reviewer Gate run. lint_spine.py: 0 findings. Three semantic lenses run inline (not as subagents — agent spawning is disabled in this session; noted as a caveat since a fresh reviewer context finds what the author talks past): version/reality check, adversarial divergence hunt, good-spine rubric walker. Reviews written to reviews/.
- (version) CORRECTION (web-verified 2026-07-24, supersedes the earlier entry): DRF added Django 6.0 support in 3.17.0 (March 2026), NOT 3.16.1. The earlier search result asserting 3.16.1 was unreliable. Correct pin if Django 6.0 survives: djangorestframework>=3.17.0.
- (version) BLOCKER (web-verified 2026-07-24 via pypi.org and the jazzband changelog): djangorestframework-simplejwt is at 5.5.1, released 2025-07-21 — ~12 months with no release. PyPI trove classifiers list Django 4.2, 5.0, 5.1, 5.2. Django 6.0 is NOT listed and the changelog never mentions it. This blocks the Django 6.0 bump because AD-14 makes SimpleJWT the entire auth invariant and the shipped code already imports rest_framework_simplejwt.tokens.RefreshToken.
- (version) Web-verified 2026-07-24: @hey-api/openapi-ts is at 0.99.0 (published ~June 2026), actively released. Pre-1.0, so it must be pinned exactly — a caret range does not protect a 0.x major.
- (decision) Reviewer-gate autofixes applied to the spine: (1) AD-4 now ENUMERATES the query-param grammar (repeated tag params ANDed, length in seconds, explicit date_field selector, unknown param = 400) — 'one documented grammar' was unenforceable while undocumented; (2) new AD-19 fixes tag identity (normalised, case-insensitive unique per user, display form preserved) and tag mutation semantics (explicit add/remove deltas ONLY, no set-replacing tags array on any resource); (3) AD-5 reworded to separate recorded facts (watched_at, deleted_at, availability) from derived states, resolving its contradiction with AD-15; (4) AD-11 now names the tsvector refresh SITE — explicit service call, never a post_save signal (signals skip bulk_create/update/bulk_update, i.e. exactly the bulk-tagging path) nor a DB trigger; (5) AD-6 now requires the outbox drain to run at the START of a run as well as after import, so orphans do not lag a full cycle; (6) conventions gained a custom DRF EXCEPTION_HANDLER rule, a bulk endpoint naming rule, and a client-observed-state rule for FR-19 play-time detection; (7) Deferred widened from 'Next.js version posture' to the whole inherited frontend pin block.
- (question) OPEN — BLOCKER for the user: SimpleJWT does not support Django 6.0, contradicting the earlier decision to bump. Options: stay on Django 5.2 LTS (SimpleJWT 5.5.1 supports it), bump anyway and run auth on an unsupported combination, or bump and replace SimpleJWT.
- (question) OPEN — rubric walker found a SILENT DIMENSION: durability of the sole copy. PRD §8 names local durability as an NFR, FR-6 deletes the YouTube copy after import, FR-4 (P2) deletes whole playlists upstream, and AD-17 puts the only remaining copy in an unbacked Docker volume on one laptop. 'docker compose down -v' or a disk failure loses the entire curated library. The spine is silent — neither decided nor deferred.
- (question) OPEN — package manager (rubric walker, HIGH): FRONTEND-STACK.md mandates pnpm + phantom-dependency discipline; repo uses npm; AD-2 removed the workspace motivation. Two parallel PRs adding deps under different managers produce competing lockfiles. Needs a one-line answer.
- (decision) BLOCKER RESOLVED (user chose): bump to Django 6.0.x AND replace djangorestframework-simplejwt. Token issue/verify moves to a first-party organizer/auth/ module over PyJWT 2.13.0 (web-verified: released 2026-05-21, healthy cadence, framework-agnostic so no Django coupling), exposed to DRF as a single custom authentication class that reads the access_token cookie directly. JWTAuthCookieMiddleware's global header-injection is RETIRED with SimpleJWT — the auth class reads the cookie itself. The cookie contract with the frontend is unchanged; only the backend implementation moves. DRF pin corrected to >=3.17.0.
- (decision) DURABILITY (user chose) — new AD-20: a make backup pg_dump target exists from the first data-model story, a documented restore path is verified once before Phase 1 is done, the Postgres volume is named and never destroyed by any routine command, and FR-4's irreversible YouTube-side playlist deletion is GATED on a recent successful backup. Import and inbox-clear are deliberately not gated (gating the weekly loop would break it). Closes the silent dimension the rubric walker found.
- (decision) PACKAGE MANAGER (user chose): npm with package-lock.json for the frontend; pip + requirements.txt for the backend. pnpm and its phantom-dependency discipline are dropped along with the workspace that motivated them. Docs/FRONTEND-STACK.md to be amended.
- (event) Reviewer Gate closed. lint_spine.py re-run after fixes: 0 findings. AD-20 added, AD ordering corrected to monotonic, spine frontmatter set to status: final.
- (event) WORK-SPLIT.md produced (second requested deliverable): 14 Phase-1 epics with governing ADs and a dependency graph, critical path E1->E5->E6->E7->E13, plus a non-decomposed Phase-2 table. Spine finalized.
