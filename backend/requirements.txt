Django==6.0.8
djangorestframework==3.18.0
django-cors-headers==4.9.0
drf-spectacular==0.30.0
google-auth==2.56.3
google-auth-oauthlib==1.4.0
google-auth-httplib2==0.4.1
google-api-python-client==2.198.0
psycopg2-binary==2.9.12
requests==2.34.2
django-extensions==4.1
Werkzeug==3.1.8
pyOpenSSL==26.4.0
coverage==7.15.4
# First-party token issue/verify (AD-14, Story 1.3). No [crypto] extra: HS256 is
# symmetric, so `cryptography` would only be needed for RS*/ES*/PS*.
PyJWT==2.13.0
# Read by organizer/tests/test_durability.py, which parses docker-compose.yml to
# assert the Postgres volume's explicit name (AD-20, Story 1.5). Present before as a
# transitive of drf-spectacular; pinned directly now that first-party code imports it.
PyYAML==6.0.3
